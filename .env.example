# The CLI writes these values to its private config file; environment variables
# are only useful for scripted installer defaults.
AGENT_MESH_HUB_URL=http://127.0.0.1:3100
AGENT_MESH_LANE_ID=my-agent
AGENT_MESH_RUNTIME=claude
